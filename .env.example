# Root authentication credentials for user registration (POST /auth/register)
AUTH_ROOT_USERNAME=admin
AUTH_ROOT_PASSWORD=admin123

# Squid proxy test target (HTTP URL to test through proxy)
SQUID_HTTP_TARGET=https://httpbin.org/get

# Optional: Database file path (default: backend/data.db)
# DATABASE_URL=backend/data.db
